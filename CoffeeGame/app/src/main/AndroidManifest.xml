<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liaiwei.coffeegame">

    <application
        android:allowBackup="true"
        android:icon="@drawable/frappeicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/frappeicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page1" />
        <activity android:name=".page2" />
        <activity android:name=".page3" />
        <activity android:name=".page4"></activity>
    </application>

</manifest>